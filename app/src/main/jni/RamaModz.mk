#include "RAMA_MODZ"
#include "RamaModz"
    struct _esp {
        bool Box;
        bool Line;
        bool Health;
        bool Jarak;
        bool LineRgb;
        bool Hexagono;
        bool Distancia;
        bool Distance;
        bool IsName;
        bool Path;       
        bool Esp360;
        bool Target;
    }